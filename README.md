# QR Scan — MERN example

Two folders: `server` (Express + Mongo) and `client` (React + Vite).

Run server:

```
cd server
npm install
cp .env.example .env
# set MONGO_URI if needed
npm run dev
```

Run client:

```
cd client
npm install
npm run dev
```

Open `http://localhost:3000`.
